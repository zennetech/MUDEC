<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projeto DCEI - Zenne</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

        :root {
            --primary-color: #6a11cb;
            --secondary-color: #2575fc;
            --background-color: #f0f2f5;
            --text-color: #333;
            --card-bg: #ffffff;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            --border-radius: 12px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* --- Header & Hero --- */
        .hero {
            background-image: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 100px 0;
            text-align: center;
            border-bottom-left-radius: 50px;
            border-bottom-right-radius: 50px;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 10px;
            animation: fadeInDown 1s ease-out;
        }

        .hero p {
            font-size: 1.25rem;
            font-weight: 300;
            max-width: 600px;
            margin: 0 auto 30px;
            animation: fadeInUp 1s ease-out 0.5s;
            animation-fill-mode: backwards; /* Inicia a animação corretamente */
        }

        .hero-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            animation: fadeInUp 1s ease-out 1s;
            animation-fill-mode: backwards;
        }

        .btn {
            padding: 12px 28px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-primary {
            background-color: #fff;
            color: var(--primary-color);
        }

        .btn-secondary {
            background-color: transparent;
            color: #fff;
            border: 2px solid #fff;
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.2);
        }
        
        /* --- Seções de Conteúdo --- */
        .content-section {
            padding: 80px 0;
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }

        .content-section.visible {
            opacity: 1;
            transform: translateY(0);
        }
        
        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 50px;
            position: relative;
        }
        
        .section-title::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background-image: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            margin: 10px auto 0;
            border-radius: 2px;
        }

        /* --- Grid de Features --- */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .feature-card {
            background: var(--card-bg);
            padding: 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }

        .feature-card .icon {
            font-size: 3rem;
            margin-bottom: 20px;
            color: var(--primary-color);
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }
        
        /* --- Seção Sobre --- */
        #about {
            background-color: var(--card-bg);
        }
        
        .about-content {
            display: flex;
            align-items: center;
            gap: 50px;
            flex-wrap: wrap;
        }
        
        .about-text {
            flex: 1 1 50%;
        }

        .about-image {
            flex: 1 1 40%;
            text-align: center;
        }

        .about-image img {
            max-width: 100%;
            height: auto;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
        }

        /* --- Footer --- */
        .footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 30px 0;
            margin-top: 50px;
        }

        .footer p {
            font-weight: 300;
        }
        
        /* --- Animações --- */
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* --- Responsividade --- */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            .hero p {
                font-size: 1rem;
            }
            .about-content {
                flex-direction: column;
            }
        }

    </style>
</head>
<body>

    <header class="hero">
        <div class="container">
            <h1>Projeto DCEI</h1>
            <p>
                Uma breve e impactante descrição do seu projeto. Explique o que ele faz,
                qual problema resolve e por que ele é incrível.
            </p>
            <div class="hero-buttons">
                <!-- Substitua '#' pelo link para o seu repositório -->
                <a href="https://github.com/zenne/dcei" class="btn btn-primary" target="_blank" rel="noopener noreferrer">Ver no GitHub</a>
                <!-- Adicione um link para uma demonstração ao vivo se aplicável -->
                <a href="#" class="btn btn-secondary">Demonstração</a>
            </div>
        </div>
    </header>

    <main>
        <section id="features" class="content-section">
            <div class="container">
                <h2 class="section-title">Funcionalidades Principais</h2>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="icon">🚀</div>
                        <h3>Desempenho Otimizado</h3>
                        <p>Arquitetura pensada para velocidade e eficiência, garantindo uma experiência de usuário fluida.</p>
                    </div>
                    <div class="feature-card">
                        <div class="icon">🎨</div>
                        <h3>Design Flexível</h3>
                        <p>Componentes reutilizáveis e um design system que permite fácil customização e extensão.</p>
                    </div>
                    <div class="feature-card">
                        <div class="icon">🔒</div>
                        <h3>Segurança em Primeiro Lugar</h3>
                        <p>Implementação de práticas modernas de segurança para proteger dados e usuários.</p>
                    </div>
                </div>
            </div>
        </section>
        
        <section id="about" class="content-section">
            <div class="container">
                <h2 class="section-title">Sobre o Projeto</h2>
                <div class="about-content">
                    <div class="about-text">
                        <h3>Nossa Motivação</h3>
                        <p>Aqui você pode detalhar a história por trás do projeto. Fale sobre a equipe, a tecnologia utilizada (por exemplo, HTML, CSS, JavaScript, etc.) e os objetivos a longo prazo. É um ótimo espaço para conectar o visitante com a sua visão.</p>
                        <p style="margin-top: 15px;">Este modelo foi criado para ser facilmente adaptável. Sinta-se à vontade para modificar cada seção para que ela se ajuste perfeitamente ao conteúdo do seu repositório.</p>
                    </div>
                    <div class="about-image">
                        <!-- Substitua a imagem por uma captura de tela do seu projeto ou um logo -->
                        <img src="https://via.placeholder.com/450x300.png?text=Imagem+do+Seu+Projeto" alt="Visão geral do projeto">
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="container">
            <p>© 2025 Zenne. Todos os direitos reservados.</p>
        </div>
    </footer>

    <script>
        // Script para animação de scroll suave
        document.addEventListener('DOMContentLoaded', () => {
            const sections = document.querySelectorAll('.content-section');

            const observer = new IntersectionObserver(entries => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        // Opcional: parar de observar após a primeira vez
                        // observer.unobserve(entry.target);
                    }
                });
            }, {
                threshold: 0.1 // A animação começa quando 10% da seção está visível
            });

            sections.forEach(section => {
                observer.observe(section);
            });
        });
    </script>

</body>
</html>
